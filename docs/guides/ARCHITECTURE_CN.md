# ILAL 架构文档

## 系统概览

ILAL (Institutional Liquidity Access Layer) 是一个基于 Uniswap v4 Hooks 的合规流动性解决方案，使用零知识证明技术在保护隐私的同时验证机构用户身份。

```
┌─────────────────────────────────────────────────────────────┐
│                          用户层                              │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐                 │
│  │ 交易者  │    │   LP    │    │ 治理者  │                 │
│  └────┬────┘    └────┬────┘    └────┬────┘                 │
└───────┼──────────────┼──────────────┼───────────────────────┘
        │              │              │
┌───────▼──────────────▼──────────────▼───────────────────────┐
│                      前端 DApp (Next.js)                     │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐   │
│  │ 验证流程 UI   │  │  交易界面     │  │  LP 管理      │   │
│  │ (ZK Proof生成)│  │  (Swap/Add)   │  │  (Position)   │   │
│  └───────────────┘  └───────────────┘  └───────────────┘   │
└─────────────────────────────────────────────────────────────┘
        │                      │                      │
        │                      │                      │
┌───────▼──────────────────────▼──────────────────────▼───────┐
│                      Base 区块链                             │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              核心合约层                                 │ │
│  │                                                          │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │ │
│  │  │ Registry │  │ Session  │  │ Verifier │             │ │
│  │  │ (UUPS)   │  │ Manager  │  │ (PLONK)  │             │ │
│  │  │          │  │ (UUPS)   │  │          │             │ │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘             │ │
│  │       │             │             │                    │ │
│  │       └─────────────┼─────────────┘                    │ │
│  │                     │                                  │ │
│  │           ┌─────────▼─────────┐                        │ │
│  │           │  ComplianceHook   │                        │ │
│  │           │  (EIP-712 验证)   │                        │ │
│  │           └─────────┬─────────┘                        │ │
│  │                     │                                  │ │
│  └─────────────────────┼──────────────────────────────────┘ │
│                        │                                     │
│  ┌─────────────────────▼──────────────────────────────────┐ │
│  │              Uniswap v4 层                              │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │ │
│  │  │PoolManager│ │ Universal │ │ Position │             │ │
│  │  │           │ │ Router    │ │ Manager  │             │ │
│  │  └──────────┘  └──────────┘  └──────────┘             │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
        │                      │                      │
        │                      │                      │
┌───────▼──────────────────────▼──────────────────────▼───────┐
│                   The Graph 子图                             │
│  索引事件 → 聚合统计 → 提供 GraphQL API                      │
└─────────────────────────────────────────────────────────────┘
```

## 核心组件

### 1. Registry (注册表)

**功能**: 系统配置中心

- 管理可信 Issuer (Coinbase, Circle 等)
- 维护路由器白名单 (Universal Router)
- 设置全局参数 (Session TTL)
- 紧急暂停开关

**技术特性**:
- UUPS 代理模式（可升级）
- Ownable 权限控制（多签治理）
- 事件日志完整

**关键接口**:
```solidity
function registerIssuer(bytes32 issuerId, address attester, address verifier);
function approveRouter(address router, bool approved);
function setEmergencyPause(bool paused);
```

### 2. SessionManager (会话管理器)

**功能**: 用户验证状态缓存

- 存储用户验证过期时间（24h TTL）
- 批量查询支持（gas 优化）
- 手动结束会话（用户主动撤销）

**技术特性**:
- UUPS 代理模式（可升级）
- AccessControl 角色管理（VERIFIER_ROLE）
- 重入保护

**关键接口**:
```solidity
function startSession(address user, uint256 expiry);
function isSessionActive(address user) returns (bool);
function endSession(address user);
```

### 3. Verifier (验证器)

**功能**: ZK 证明链上验证

- PLONK 算法验证
- 公共输入验证（用户地址、Merkle 根、Issuer 公钥）

**技术特性**:
- SnarkJS 导出的 Solidity 验证器
- Universal Setup（无需信任仪式）
- Gas 优化（~300-400k gas）

**关键接口**:
```solidity
function verifyComplianceProof(bytes calldata proof, uint256[] calldata publicInputs) returns (bool);
```

### 4. ComplianceHook (合规钩子)

**功能**: Uniswap v4 访问控制

- 拦截 `beforeSwap`, `beforeAddLiquidity`, `beforeRemoveLiquidity`
- 验证用户 Session 激活状态
- EIP-712 签名验证（hookData）

**技术特性**:
- 继承 `EIP712Verifier`
- 支持三种身份解析模式:
  1. 完整 EIP-712 签名 (前端 DApp)
  2. 空 hookData (EOA 直接调用)
  3. 仅地址 (白名单路由器)
- Nonce 防重放攻击

**关键接口**:
```solidity
function beforeSwap(address sender, bytes calldata hookData) returns (bool);
function _resolveUser(address sender, bytes calldata hookData) internal returns (address);
```

### 5. VerifiedPoolsPositionManager (受限头寸管理器)

**功能**: 防止 LP NFT 转移

- 覆盖 `safeTransferFrom` / `transferFrom`，始终回滚
- 只允许通过 DApp 管理流动性
- 确保所有 LP 都经过验证

**设计权衡**:
- ✅ 确保合规性
- ⚠️ 牺牲可组合性
- ⚠️ 用户体验受限

## 数据流

### 验证流程 (Verification Flow)

```
用户 → 前端 DApp → Coinbase → EAS → 前端
  1. 连接钱包
  2. 获取 Coinbase Verification attestation
  3. 下载 ZK 电路文件 (.wasm, .zkey)
  4. 在 Web Worker 中生成 ZK Proof (5-30s)
  5. 调用 Verifier.verifyComplianceProof(proof, publicInputs)
  6. 调用 SessionManager.startSession(user, expiry)
  7. 返回成功，缓存 Session 状态
```

### 交易流程 (Swap Flow)

```
用户 → 前端 DApp → Universal Router → ComplianceHook → Uniswap v4
  1. 用户输入交易参数
  2. 前端检查 Session 是否激活
  3. 生成 EIP-712 签名 (SwapPermit)
  4. 构造 hookData = abi.encode(user, deadline, nonce, signature)
  5. 调用 UniversalRouter.swap(..., hookData)
  6. ComplianceHook.beforeSwap() 被触发
  7. 验证 hookData 签名
  8. 检查 SessionManager.isSessionActive(user)
  9. 允许交易继续 → PoolManager 执行 swap
```

## 安全机制

### 1. 身份验证

- **ZK 证明**: 隐私保护，无需链上暴露 KYC 数据
- **EIP-712 签名**: 防止前端伪造 hookData
- **Nonce**: 防止重放攻击

### 2. 访问控制

- **Session TTL**: 24 小时自动过期
- **手动撤销**: 用户主动结束 Session
- **紧急暂停**: 治理可全局停止系统

### 3. 合约升级

- **UUPS 代理**: Registry 和 SessionManager 可升级
- **时间锁**: 建议配合 48h 时间锁使用
- **不可变合约**: Hook 和 Verifier 不可升级（安全优先）

### 4. 审计建议

| 合约 | 风险级别 | 审计重点 |
|------|---------|---------|
| Registry | 高 | 权限控制、升级逻辑 |
| SessionManager | 高 | 会话管理、角色权限 |
| ComplianceHook | 极高 | 签名验证、重入攻击 |
| Verifier | 中 | ZK 证明验证正确性 |
| PositionManager | 中 | 转移限制绕过 |

## 性能优化

### Gas 优化

| 操作 | Gas 消耗 | 优化方案 |
|------|---------|---------|
| ZK 验证 | ~350k | 已优化（PLONK） |
| Session 检查 | ~5k | 批量查询 |
| EIP-712 验证 | <10k | 最小化签名验证 |
| Swap (含 Hook) | ~200k | 无额外优化空间 |

### 前端优化

- **Web Worker**: ZK 证明生成不阻塞 UI
- **文件缓存**: `.zkey` 文件使用 IndexedDB
- **批量查询**: 一次调用获取多个用户状态

## 部署架构

### 测试网 (Base Sepolia)

```
Registry Proxy: 0x...
SessionManager Proxy: 0x...
MockVerifier: 0x...
ComplianceHook: 0x...
```

### 主网 (Base Mainnet)

```
Registry Proxy: 0x...
SessionManager Proxy: 0x...
PlonkVerifier: 0x...
ComplianceHook: 0x...
Governance Multisig: 0x... (3/5)
```

## 监控指标

### 关键指标 (KPIs)

- **总验证用户数**: 累计通过 KYC 的用户
- **活跃 Session 数**: 当前有效会话
- **每日交易量**: 通过 Hook 的交易总额
- **TVL**: 锁定在合规池的总价值

### 告警阈值

- Session 过期率 > 20%
- Hook 拒绝率 > 5%
- Gas 价格 > 100 Gwei
- 紧急暂停被触发

## 升级路线图

### Phase 1 (当前)
- ✅ 核心合约实现
- ✅ ZK 电路框架
- ✅ 前端 DApp

### Phase 2 (Q2 2026)
- 🔄 真实 PLONK Verifier 集成
- 🔄 多 Issuer 支持 (Circle, Polygon ID)
- 🔄 批量 Session 管理优化

### Phase 3 (Q3 2026)
- 📅 跨链支持 (Optimism, Arbitrum)
- 📅 高级合规规则 (国家黑名单)
- 📅 Layer 2 ZK Rollup 集成

## 参考资料

- [Uniswap v4 文档](https://docs.uniswap.org/contracts/v4/overview)
- [PLONK 算法论文](https://eprint.iacr.org/2019/953.pdf)
- [EIP-712 规范](https://eips.ethereum.org/EIPS/eip-712)
- [Coinbase Verifications](https://www.coinbase.com/onchain-verify)
- [Base 区块链](https://base.org)
