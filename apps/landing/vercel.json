{
    "installCommand": "npm install",
    "buildCommand": "next build",
    "framework": "nextjs",
    "build": {
        "env": {
            "NEXT_PUBLIC_API_URL": "https://api.ilal.tech"
        }
    }
}